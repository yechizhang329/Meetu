# Meetu Agent 协作协议

> **强制读取**：本协议是所有Meetu agent（Fiona/Lucy/Phoebe2）启动后的强制前置。
>
> **维护人**：Fiona (PM) / 协议变更需Jonathan或DavidC批准
> **生效**：2026-04-28

---

## 1. 你是谁

如果你是Meetu agent（Fiona / Lucy / Phoebe2 / 未来新增），你的工作发生在 `/Users/shaolinzheng/.slock/agents/c39d733e-363c-455a-bd3f-dde6688d038b/Meetu/` 目录下。

每个agent有自己的 `MEMORY.md`（个人记忆）+ 共享的 `Common Sense/` 目录（项目大脑）。

---

## 2. 启动后必读（按顺序）

每次启动你必须：

### Step 1: 读自己的MEMORY.md
理解自己的角色、过往feedback、个人偏好。

### Step 2: 读 Common Sense 5层（按需）

| 层 | 路径 | 何时必读 |
|----|------|---------|
| L1 产品定义 | `Common Sense/产品定义/` `商业模型/` `用户认知/` | 涉及产品决策时 |
| L1 品牌共识 | `Common Sense/品牌共识/` | 涉及视觉/语言/品牌时 |
| L2 项目状态 | `Common Sense/项目状态/` | **每次启动必读** — 知道项目今天在哪 |
| L3 决策Log | `Common Sense/决策Log/2026-MM.md` | 决策前查相关历史 |
| L4 数据归档 | `Common Sense/数据归档/` | 决策需数据支持时 |
| L5 团队与协作 | `Common Sense/团队与协作/` | **每次启动必读** — 知道谁负责什么 |

**最低门槛**：L2项目状态 + L5团队与协作 — 这两层每次启动必读。

### Step 3: 检查相关Skills

📌 **Skill图谱**：所有skill的归属/依赖/触发条件 见 [Common Sense/团队与协作/skill图谱.md](Common%20Sense/团队与协作/skill图谱.md)

| Skill | 何时调用 |
|-------|---------|
| `xhs-language-research` | 写大学生XHS文案前必读 |
| `xhs-content-checklist` | 文案发布前必跑 |
| `write-xiaohongshu` | 写XHS笔记的总流程 |
| `qiushi-skill` 系列 | 战略分析/方法论使用 |
| `activity-product-definition` | 设计/审视活动产品 |

完整skill图谱+依赖关系+触发场景见上方文档链接。

**重要原则**（基于skill graph 2.0借鉴）：
- 不是所有skill每次都要加载——按"触发条件"调用
- 调用skill前先看skill图谱里它的"前置依赖"是否已满足
- 跨agent调用skill前确认是不是自己角色范围内的skill

---

## 3. 决策与执行规则

### 3.1 决策前查
做任何项目级决策前 必查：
- L3 决策Log — 历史上有没有相关决策（避免推翻或重复）
- L1 产品定义 — 决策是否符合产品边界
- L4 数据归档 — 有没有数据支撑

### 3.2 决策后录
任何项目级决策**当天**记入：
- L3 决策Log（按月归档）
- 如果是阶段切换 → 同步更新L2 项目状态/当前阶段.md
- 如果是数据触发 → 同步更新L4 数据归档对应文件

**记录责任**：
- 战略级决策（决策层做的）：PM (Fiona) 负责录入
- 执行级决策（agent间）：发起人录入

### 3.3 知识沉淀
重要洞察（如"是个正常人"）**不能只在channel里说**：
- 立即录入决策Log
- 适合静态认知的（如用户原话）→ 同步更新L1对应文件
- 形成方法论的 → 写成skill

---

## 4. 跨Agent协作SOP

### 4.1 频道分工
- `#产品讨论Product` — 产品/战略/PM主导
- `#运营讨论Operations` — 运营/小红书/Lucy主导
- `#设计讨论Design` — 视觉/品牌/Phoebe2主导
- `#all` — 多agent协作 不发单任务
- `dm:@person` — 1对1沟通

### 4.2 PM作为通信桥梁
跨频道任务由PM (Fiona) 转译分发——不让执行层跨频道看未确认内容。

### 4.3 Review流程
内容产出后：
1. 产出方自查（按对应checklist）
2. PM review（调性/边界/差异化）
3. 决策层确认（Jonathan/DavidC最终拍板）

### 4.4 能力边界（基于2026-04-26方法论决策）

各agent明确"做不稳的事"：

| Agent | 做不稳 | 应该走 |
|-------|------|------|
| Fiona | 视觉设计/具体文案润色 | 战略+协调+review |
| Lucy | 第一人称大学生原创情绪故事 / 个人成长弧线型 | 数据型/清单型/讨论激发型/真人素材润色 |
| Phoebe2 | 没有真实素材的设计探索 / 无环境支持的工具调用 | 基于brief的视觉执行/prompt产出 |

**承认边界比硬撑更重要**——这条已是协作铁律。

---

## 5. 项目状态同步原则

### 5.1 Static vs Dynamic分清
- 静态认知（产品定义/品牌/用户画像）→ 几个月才变
- 动态状态（项目阶段/本周聚焦/数据）→ 至少每周更新

### 5.2 PM每周日更新本周聚焦
本周完成什么 / 下周聚焦什么 → `项目状态/本周聚焦.md`

### 5.3 重大节点更新当前阶段
战略阶段切换/主要矛盾变化 → `项目状态/当前阶段.md`

---

## 6. 失效情况

如果你启动后发现以下情况 立即停止工作并报告：
- ❌ Common Sense 5层内容明显过期（>2周未更新核心动态）
- ❌ 决策Log有空白月份
- ❌ 数据归档跟channel历史明显不符
- ❌ 你被指派的工作跟L2本周聚焦/L5协作SOP冲突

报告对象：发起任务的决策层 + Fiona

---

## 7. 协议版本

| 版本 | 日期 | 变更 |
|------|------|------|
| v1 | 2026-04-28 | 初版 — Common Sense升级到5层 + Agent启动协议 |

---

**协议解释权**：Fiona (PM) / 重大变更需Jonathan或DavidC批准。
